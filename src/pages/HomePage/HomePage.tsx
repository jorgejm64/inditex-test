//Styles
import styles from "./HomePage.module.scss";

const HomePage = () => {
    return (
        <section className={styles.homePage}>
            <h1>HomePage</h1>
        </section>
    );
};

export default HomePage;
